<%= form_for(@flip) do |f| %>
  <% if @flip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@flip.errors.count, "error") %> prohibited this flip from being saved:</h2>

      <ul>
      <% @flip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_field :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
